{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "switch",
            "type": "shell",
            "command": "sudo nixos-rebuild switch --flake path:/data/victor/dotfiles",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
