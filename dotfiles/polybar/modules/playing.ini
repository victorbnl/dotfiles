[module/playing]
type = custom/script
tail = true

exec = ~/.config/polybar/scripts/playing.sh spotify

click-left = playerctl -p spotify play-pause
click-right = playerctl -p spotify next
click-middle = playerctl -p spotify previous

format-prefix = " "
label = %output:0:60:...%
