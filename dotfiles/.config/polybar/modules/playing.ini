[module/playing]
type = custom/script
tail = true

exec = playerctl -F metadata title

click-left = playerctl play-pause
click-right = playerctl next
click-middle = playerctl previous

format-prefix = " "
label = %output:0:60:...%
